<Window
    x:Class="LiveCaptionsTranslator.OllamaChatWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Ollama Assistant" Height="400" Width="450"
    WindowStartupLocation="CenterScreen"
    Background="#202020">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ollama AI 助手" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="#333333" CornerRadius="5" Padding="10" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="接收到的外语原文：" Foreground="#AAAAAA" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="SourceTextBox" Style="{x:Null}" IsReadOnly="True" TextWrapping="Wrap" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="14"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="#2B2B2B" CornerRadius="5" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Ollama 回复：" Foreground="#AAAAAA" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="ResponseTextBox" Style="{x:Null}" Grid.Row="1" IsReadOnly="True" TextWrapping="Wrap" Background="Transparent" Foreground="#E0E0E0" BorderThickness="0" FontSize="14" VerticalScrollBarVisibility="Auto"/>
            </Grid>
        </Border>
    </Grid>
</Window>